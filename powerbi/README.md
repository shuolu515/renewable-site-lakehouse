# Power BI deliverable

The final dashboard will consume Gold site-assessment and pipeline-audit tables. It will show
candidate ranking, estimated PV potential, score components, source coverage, quarantine counts and
pipeline status. A PBIX file will only be added after checking that it embeds no restricted data or
credentials.

